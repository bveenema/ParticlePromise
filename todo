Update lambda example for in class situation

Check if Subscribe is enabled before creating new (bool isSubscribed)
  Add bool return from enable() - true if Subscribe returns true DONE
  Always return dummy Promise if isSubscribed = false DONE

enforce unique response topics (have to look into overhead)
